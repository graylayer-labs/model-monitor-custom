# mmc-analyser-mq

Phase 3.4 skeleton. `NoopMqAnalyser` returns canned output to prove the
base+analyser plumbing extends beyond bias. Real MQ (model quality) math
lands in Phase 5.

See `docs/design/002-container-base.md`.
