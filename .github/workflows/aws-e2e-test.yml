name: AWS E2E Test

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      region:
        description: "AWS region"
        required: false
        default: "eu-west-1"
        type: string
      no_cleanup:
        description: "Keep AWS resources after test (for debugging)"
        required: false
        default: false
        type: boolean

permissions:
  contents: read
  id-token: write  # Required for OIDC

jobs:
  aws-e2e-test:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ inputs.region || 'eu-west-1' }}

      - name: Get AWS Account ID
        run: echo "AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)" >> $GITHUB_ENV

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        run: |
          uv sync --group dev
          cd cdk && uv sync

      - name: Run AWS E2E test
        env:
          AWS_REGION: ${{ inputs.region || 'eu-west-1' }}
          AWS_ACCOUNT_ID: ${{ env.AWS_ACCOUNT_ID }}
        run: |
          python3 scripts/aws-e2e-test.py \
            --region "$AWS_REGION" \
            ${{ inputs.no_cleanup && '--no-cleanup' || '--cleanup' }} \
            --verbose

      - name: Report results
        if: always()
        run: |
          echo "AWS E2E test execution complete"
          echo "Region: ${{ inputs.region || 'eu-west-1' }}"
          echo "Cleanup: ${{ !inputs.no_cleanup }}"
          echo "AWS Account: ${{ env.AWS_ACCOUNT_ID }}"

      - name: Cleanup on failure
        if: failure() && !inputs.no_cleanup
        env:
          AWS_REGION: ${{ inputs.region || 'eu-west-1' }}
        run: |
          cd cdk
          uv run cdk destroy '*' --force 2>/dev/null || true
          echo "Cleanup completed"
