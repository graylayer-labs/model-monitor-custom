projects:
  - name: mmc-aws-test
    inference_account: "YOUR_ACCOUNT_ID"
    producer_bucket_arn: "arn:aws:s3:::mmc-aws-test-producer"
    compute_backend: lambda
    vpc_id: null
    schedule: "cron(0 * * * ? *)"
